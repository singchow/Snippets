<h2>
  <%= link_to 'Create Snippet', new_snippet_path, class:"btn btn-default", type:"button" %>
</h2>

<% if(@personalsnippets.blank?) %>
  <h4>You have not created any snippets.</h4>
<% else %>
  <table>
    <thead>
      <tr>
        <th colspan="3">Snippet Title</th>
        <th colspan="3">Snippet Description</th>
        </tr>
    </thead>

    <tbody>
      <% @personalsnippets.each do |snippet| %>
        <tr>
          <td colspan="3" id="redhover"><%= link_to snippet.snippet_title, snippet %></td>
          <td colspan="3"><%= snippet.snippet_description %></td>
          <!-- <td><% link_to 'Edit', edit_snippet_path(snippet) %></td>
          <td><%= link_to 'Destroy', snippet, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
